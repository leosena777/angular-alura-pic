import { PhotoOwnerOnltyDirective } from './photo-owner-onlty.directive';

describe('PhotoOwnerOnltyDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoOwnerOnltyDirective();
    expect(directive).toBeTruthy();
  });
});
